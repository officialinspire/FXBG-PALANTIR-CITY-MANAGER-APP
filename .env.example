PORT=3001
LOG_DIR=logs

# Required for Air Quality (WAQI/AQICN).
WAQI_TOKEN=your-waqi-token

# Required for UV index data.
OPENUV_API_KEY=your-openuv-key

# Optional (CDC data access token, if available).
CDC_APP_TOKEN=
